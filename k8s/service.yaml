apiVersion: v1
kind: Service
metadata:
  name: flight-delay-api-service
  labels:
    app: flight-delay-api

spec:
  type: NodePort

  selector:
    app: flight-delay-api

  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
